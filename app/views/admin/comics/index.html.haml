= provide(:title, 'Comics') 

.page-header
  %h1
    List of Comics
  
%table.table.table-bordered{:cellpadding=>10, :width=>'100%'}
  %tr
    %th
      Order
    %th
      Title 
    %th
      Actions 

  - @comics.sort_by{|c| c.order_id}.each do |comic| 
    %tr
      %td
        = comic.order_id
      %td
        = comic.title 
      %td{:width=>'20%'}
        %span{:style => 'white-space: nowrap'}
          .center
            = action_links [:admin, comic], :exclude => [:index], :delete => {:title=>'Delete', :alt_text=>'Delete', :confirm=> 'Are you sure you want to delete this comic?'}

%br 

= link_to 'Create New Comic', new_admin_comic_path 
